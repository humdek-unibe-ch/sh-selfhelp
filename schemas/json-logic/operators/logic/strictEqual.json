{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "file:///schemas/json-logic/operators/logic/strictEqual.json",

  "title": "Strict Equal",
  "description": "Tests strict equality. Requires two arguments.",

  "type": "object",
  "additionalProperties": false,
  "required": ["==="],
  "properties": {
    "===": {
      "$ref": "../../common/binary-args.json"
    }
  },
  "examples": [
    { "===": [1, 1] },
    { "===": [1, "1"] }
  ]
}
