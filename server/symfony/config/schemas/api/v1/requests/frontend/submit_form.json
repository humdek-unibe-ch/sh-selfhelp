{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Submit Form Request",
  "description": "Schema for form submission request",
  "required": ["page_id", "section_id", "form_data"],
  "properties": {
    "page_id": {
      "type": "integer",
      "description": "The ID of the page containing the form",
      "minimum": 1
    },
    "section_id": {
      "type": "integer",
      "description": "The section ID of the form",
      "minimum": 1
    },
    "form_data": {
      "type": "object",
      "description": "The form data to be submitted",
      "minProperties": 1,
      "additionalProperties": {
        "oneOf": [
          {"type": "string", "maxLength": 65535},
          {"type": "number"},
          {"type": "boolean"},
          {"type": "null"}
        ]
      },
      "patternProperties": {
        "^[a-zA-Z][a-zA-Z0-9_]{0,254}$": true
      }
    }
  },
  "additionalProperties": false
}
