{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Update Section in Page Request",
    "description": "Schema for updating a section's position within a page",
    "type": "object",
    "properties": {
        "position": {
            "type": ["integer", "null"],
            "description": "The new position of the section within the page. If null, the section's position is effectively unset (if the application logic supports this) or it might be moved to the end."
        }
    },
    "additionalProperties": false
}
