{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Create Page Section Request",
    "description": "Request schema for creating a new section on a page",
    "type": "object",
    "properties": {
        "position": {
            "type": "integer",
            "description": "The position of the section on the page",
            "minimum": 0
        },
        "styleId": {
            "type": "integer",
            "description": "The ID of the style to use for the section"
        }
    },
    "required": ["position", "styleId"]
}
