{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Create Child Section Request",
    "description": "Request schema for creating a new child section inside another section",
    "type": "object",
    "properties": {
        "position": {
            "type": "integer",
            "description": "The position of the child section within the parent section"
        },
        "styleId": {
            "type": "integer",
            "description": "The ID of the style to use for the section"
        }
    },
    "required": ["position", "styleId"]
}
