{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Form Updated Response",
  "description": "Schema for successful form update response",
  "required": ["record_id", "section_id", "page_id", "updated_at"],
  "properties": {
    "record_id": {
      "type": "integer",
      "description": "The ID of the updated record",
      "minimum": 1
    },
    "section_id": {
      "type": "integer",
      "description": "The section ID that was updated",
      "minimum": 1
    },
    "page_id": {
      "type": "integer",
      "description": "The ID of the page containing the form",
      "minimum": 1
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the form was updated"
    }
  },
  "additionalProperties": false
}
